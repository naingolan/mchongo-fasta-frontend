export const environment = {
  production: true,
  /**
   * Production API origin (no trailing slash).
   * After deploying `backend/` to Vercel, set this to that URL, e.g.
   * `https://mchongofasta-api.vercel.app`
   *
   * Leave empty only if `web/vercel.json` rewrites `/api/*` to the API project.
   */
  apiUrl: '',
};
